node_modules
/dist
.vite
.DS_Store
.env
.env.local

# Uploaded movement evidence (receipts, invoices, photographs)
server/uploads


.env*

# Vercel CLI project linkage (org and project ids) -- local, never committed
.vercel
